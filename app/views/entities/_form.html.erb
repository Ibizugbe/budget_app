<%= form_with(model: [@group, @entity]) do |form| %>
  <% if entity.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(entity.errors.count, "error") %> prohibited this transaction from being saved:</h2>

      <ul>
        <% entity.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, class: "form-control border-success my-1 p-2" %>
  </div>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount, class: "form-control border-success my-1 p-2" %>
  </div>



  <div>
    <%= form.submit "Save Transaction", class: "btn btn-success my-3 px-5" %>
  </div>


<% end %>


<div class="actions_expense">
  <% if @entity.id != nil && current_page?(edit_group_entity_path(@group, @entity)) %>
    <%= button_to "Delete transaction", group_entity_path(@group, @entity), method: :delete, class: "btn btn-danger px-4" %>
  <% end %>
</div>